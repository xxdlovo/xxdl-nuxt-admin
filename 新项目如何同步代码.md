# 基础代码同步方案（私有仓库替代 Fork）
## 方案说明
因为 Fork 公共仓库后，无法将业务仓库设置为私有，因此改用**业务仓库内添加上游远程**的方式实现代码同步。

整体思路：在私有业务仓库中新建纯净同步分支，完整继承上游基础仓库提交历史；上游有更新时，先在纯净分支完成合并，再将变更合并到业务主分支 `main`，在私有仓库内维护可正常合并的基础代码副本。

> ✨ 本次重构优化：不再使用 `--orphan` 孤立分支创建方式，直接基于上游 `upstream/main` 创建 `base-sync`，上下游拥有共同提交祖先，后续合并不会报 `refusing to merge unrelated histories` 无关历史错误，WebStorm 图形菜单可全程正常使用。

## 一、首次初始化（仅执行一次）
在你的私有业务仓库执行，添加上游远程并创建纯净同步分支 `base-sync`
```bash
# 1. 添加上游基础仓库远程
git remote add upstream https://github.com/xxdlovo/xxdl-nuxt-admin.git
git fetch upstream

# 2. 基于上游 upstream/main 直接创建纯净同步分支 base-sync（继承完整上游提交历史）
git checkout -b base-sync upstream/main

# 3. 将分支推送到远程仓库，绑定上游追踪关系
git push -u origin base-sync
```
> 说明：`git checkout -b base-sync upstream/main` 代表以上游 main 分支为基准新建本地 `base-sync`，完整继承上游所有提交记录，上下游存在共同祖先，后续合并无历史报错。

## 二、日常同步上游更新流程
上游基础仓库发生变更后，执行下面流程同步代码
```bash
# 1. 切换至纯净同步分支
git checkout base-sync
# 更新上游仓库最新提交
git fetch upstream
# 合并上游最新变更（仅基础代码，无业务代码，冲突更少）
git merge upstream/main
# 解决冲突后，提交并推送到远程
git push origin base-sync

# 2. 将纯净基础变更合并至业务主分支 main
git checkout main
git merge base-sync
# 解决业务代码与基础代码冲突，提交推送后继续业务开发
git push origin main
```

## 分支结构说明
```
你的私有业务仓库
├─ base-sync 【纯净同步分支】：完整继承上游提交历史，仅同步上游基础代码，不编写业务代码
└─ main      【业务主分支】：日常业务开发，定期合并 base-sync 获取基础库更新
```

## 三、WebStorm 图形界面操作补充说明
### 1. `git fetch upstream` 可视化操作
右键菜单**没有直接等价 `git fetch upstream` 的选项**，两种可视化实现方式：
1. 推荐：顶部菜单栏 `Git` → `Fetch`，等价 `git fetch --all`，一次性拉取 origin + upstream 全部远程更新
2. 仅拉取 upstream：Git日志面板选中 `upstream` 文件夹 → 右上角更多操作 → `Fetch Selected（获取选中远程）`

> Fetch 仅下载远程更新，**不会改动本地文件，属于安全操作**。

### 2. 右键菜单合并/变基选项释义
操作对象：`upstream/main`，目标分支：`base-sync`
> 变基（rebase）：整理提交线，把本地提交挪到远程最新提交之后，提交历史是一条直线
> 合并（merge）：新增一条合并提交，保留两条分支完整提交历史，会产生分叉

1. **将 'base-sync' 变基到 'upstream/main'**
   等价命令：
```bash
git checkout base-sync
git rebase upstream/main
```
含义：把 `base-sync` 自有提交截取，放在 `upstream/main` 最新提交后，整理为线性历史。

2. **将 'upstream/main' 合并到 'base-sync' 中**
   等价命令：
```bash
git checkout base-sync
git merge upstream/main
```
含义：标准普通合并，生成新合并提交，完整保留双方提交记录。

3. **使用“变基”拉入 'base-sync'**
   等价命令：
```bash
git checkout base-sync
git pull --rebase upstream main
```
含义：自动执行 fetch + rebase，一体化操作。

4. **使用“合并”拉入 'base-sync'**
   等价命令：
```bash
git checkout base-sync
git pull upstream main
```
含义：自动执行 fetch + merge，一体化操作。

### 3. 菜单选项关键对比（适配本方案）
| 菜单选项 | 对应命令 | 是否推荐本方案使用 |
|---|---|---|
| 将 'upstream/main' 合并到 'base-sync' 中 | git merge | ✅ 推荐（分步可控，与文档流程一致） |
| 将 'base-sync' 变基到 'upstream/main' | git rebase | ❌ 不推荐 |
| 使用“变基”拉入 'base-sync' | git pull --rebase | ❌ 不推荐，一体化自动操作，流程不可控 |
| 使用“合并”拉入 'base-sync' | git pull | ❌ 不推荐，一体化自动操作，流程不可控 |

### ✅ WebStorm 标准图形化同步顺序
1. `Git` → `Fetch`，拉取上游全部更新
2. 切换本地分支至 `base-sync`
3. 右键 `upstream/main` → **将 'upstream/main' 合并到 'base-sync' 中**
4. 处理冲突，提交并推送到远程 `origin/base-sync`
5. 切换到业务分支 `main`，合并 `base-sync`

> 补充建议：同步上游基础库优先普通 merge，提交历史完整可追溯；变基更适合功能分支多人协作。

## 四、优缺点 & 提交记录说明
### ✅ 优点
1. 支持仓库设置为私有，规避公共仓库 Fork 后无法私有化的限制；
2. 完整保留上游基础仓库全部原始提交记录，上下游拥有共同提交祖先，合并无 `unrelated histories` 报错；
3. 可以持续同步上游基础仓库的代码变更；
4. 基础代码与业务代码在不同分支隔离，冲突处理更清晰；
5. 全程支持 WebStorm 图形界面合并操作，无需额外参数。

### ❌ 缺点
1. 业务 `main` 分支与上游无直接关联，上游更新必须先同步至 `base-sync`，再合并到业务分支，多一步分支流转操作；

> ❓ 补充答疑：
> 上游所有历史提交、后续新增提交，均可在 `base-sync` 正常查看完整日志；
> 业务 `main` 分支自身所有业务提交记录全程不会丢失。