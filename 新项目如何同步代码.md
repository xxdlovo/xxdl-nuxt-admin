# 基础代码同步方案（私有仓库替代 Fork）
## 方案说明
因为 Fork 公共仓库后，无法将业务仓库设置为私有，因此改用**业务仓库内添加上游远程**的方式实现代码同步。

整体思路：在私有业务仓库中新建一条空白纯净分支，专门用于同步上游基础仓库代码；上游有更新时，先在纯净分支完成合并，再将变更合并到业务主分支 `main`，相当于在业务仓库内维护一份独立纯净的基础代码副本。

## 一、首次初始化（仅执行一次）
在你的私有业务仓库执行，添加上游远程并创建纯净同步分支 `base-sync`
```bash
# 1. 添加上游基础仓库远程
git remote add upstream https://github.com/xxdlovo/xxdl-nuxt-admin.git
git fetch upstream

# 2. 创建空白孤立分支 base-sync
git checkout --orphan base-sync
# 清空暂存区所有文件
git rm -rf .
# 拉取上游 main 分支全部代码至当前分支
git checkout upstream/main .
# 提交初始化记录
git commit -m "init base-sync: import upstream base code"
# 推送至远程仓库
git push -u origin base-sync
```
> 说明：`--orphan` 用于创建无任何提交历史的空白孤立分支，满足纯净分支的需求。

## 二、日常同步上游更新流程
上游基础仓库发生变更后，执行下面流程同步代码
```bash
# 1. 切换至纯净同步分支
git checkout base-sync
# 更新上游仓库最新提交
git fetch upstream
# 合并上游最新变更（仅基础代码，无业务代码，冲突更少）
git merge upstream/main
# 解决冲突后，提交并推送到远程
git push origin base-sync

# 2. 将纯净基础变更合并至业务主分支 main
git checkout main
git merge base-sync
# 解决业务代码与基础代码冲突，提交推送后继续业务开发
git push origin main
```

## 分支结构说明
```
你的私有业务仓库
├─ base-sync 【纯净同步分支】：仅同步上游基础代码，不编写业务代码
└─ main      【业务主分支】：日常业务开发，定期合并 base-sync 获取基础库更新
```

## 优缺点 & 提交记录说明
### ✅ 优点
1. 支持仓库设置为私有，规避公共仓库 Fork 后无法私有化的限制；
2. 可以持续同步上游基础仓库的代码变更；
3. 基础代码与业务代码在不同分支隔离，冲突处理更清晰。

### ❌ 缺点
1. **首次初始化时，基础仓库完整原始提交链路会丢失**：`--orphan` 创建的孤立分支没有继承上游完整提交历史，初始化之后只保留一条导入提交；

> ❓ 补充答疑：初始化完成后，**上游后续新增的提交记录可以正常保留**
> 后续每次 `git merge upstream/main` 合并进来的上游新增提交，都会完整保存在 `base-sync` 分支中，可以正常查看提交日志；
> 业务 `main` 分支自身所有业务提交记录全程不会丢失。